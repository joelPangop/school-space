spring:
  datasource:
    url: jdbc:h2:mem:schoolspace;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: root
    password: Abc123...
  security:
    user:
      name: testuser
      password: testpass
  frontend:
    url=${FRONTEND_URL}:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
